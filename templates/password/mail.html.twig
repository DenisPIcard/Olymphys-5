{# resetting/mail.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Vérification de l'adresse mail</title>
</head>
<body>
<p>
    Bonjour, <br>
    Cliquez sur ce lien pour changer votre mot de passe.
    {# pour que le lien fonctionne, il est indispensable que le lien soit absolu. #}
    <a href="{{ absolute_url(path('reset_password', {'id': user.id, 'token': user.token})) }}">Changer le mot de
        passe</a>
</p>
</body>
</html>

