<!doctype html>
{% apply inline_css %}
<html lang="fr">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width">
    <title>{{ newsletter.name }}</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f3f3f3;
            font-family: Helvetica, Arial, sans-serif;
        }

        h1 {
            background-color: #264459;
            color: #ffffff;
            padding: 5px 0 5px 0;
            font-weight: normal;
        }

        hr {
            border: none;
            border-top: 3px solid #264459;
            margin: 20px;
        }

        .container {
            background-color: #fefefe;
            width: 580px;
            margin: 0 auto;
        }

        .bottom {
            background-color: #efefee;
        }

        .block {
            margin: 0;
            padding: 10px 20px 20px 20px;
        }

        {#  .logo {
             width: 100%;
        }#}
        .text-center {
            text-align: center;
        }

        .text-left {
            text-align: left;
        }

        .btn {
            display: inline-block;
            padding: 1px 1px;
            background-color: #264459;
            color: #fefefe;
            border: 1px solid #fff;
            border-radius: 3px;
            font-size: 20px;
            font-weight: bold;
            text-decoration: none;
        }
    </style>
</head>
<body>
<div class="body">
    <div class="container">
        {# <div class="header text-left">
            <a href="{{ url('core_home') }}">
                <img src="{{ absolute_url(asset('build/images/email/logo.png')) }}" class="logo" width="50" alt="SpaceBar Logo">
            </a>
        </div> #}
        <div class="content">
            <p class="text-center"><font color="#B21B16">Olympiades de physique France</font></p>
            <h1 class="text-center">{{ newsletter.name }}</h1>
            <p class="block">
                {{ newsletter.texte|raw }}
            </p>


        </div>
        <div class="footer">
            <p class="block">
                À bientôt,<br><br>
                L'équipe d'<em>Olymphys</em>
            </p>
            <p class="block text-center">
            <h6>Vous recevez ce message car vous êtes abonné à la newsletter des OdPF, pour vous désabonner cliquez sur
                ce lien :
                {# <a href=" {{ absolute_url(path('sesincription_newslette',{userid: user.id})) }}"> Se désabonner</a>
                 adresse écrite en 'dure' car la route générée par messenger est sytématiquement tronquée de :8000 #}
                <a href=" localhost:8000/newsletter/desinscription,{{ user.id }}"> Se
                    désabonner</a> {# <a href="{{absolute_url(path('verif_mail', {'id': user.id, 'token': user.token}))}}">Se désinscrire</a> #}


                <br>Pour s'abonner de nouveau, il faudra cliquer sur la case "abonné(e) à la newsletter des OdPF" de
                votre profil.</h6>
            </p>
        </div>
    </div>
</div>
</body>
</html>
{% endapply %}